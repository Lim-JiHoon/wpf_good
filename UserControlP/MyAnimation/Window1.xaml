<Window x:Class="UserControlP.MyAnimation.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserControlP.MyAnimation"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
  <Grid Name="gridMain">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Name="col0"/>
      <ColumnDefinition Name="col1"/>
    </Grid.ColumnDefinitions>
    <StackPanel Background="Blue">
      <Button Name="btn" Click="Button_Click">
        <!--<Button.Triggers>
          <EventTrigger RoutedEvent="Button.Click" SourceName="btn">
            <BeginStoryboard Name="BeginStoryBoard" >
              <Storyboard>
                <local:GridLengthAnimation
                  Storyboard.TargetName="col0"
                  Storyboard.TargetProperty="Width"
                  FillBehavior="HoldEnd"
                  From="*" To="2*" 
                  Duration="0:0:0.5"/>
              </Storyboard>
            </BeginStoryboard>
            
          </EventTrigger>
        </Button.Triggers>-->
      </Button>
    </StackPanel>
    <Grid Grid.Column="1" Background="Red">

    </Grid>
  </Grid>
</Window>
